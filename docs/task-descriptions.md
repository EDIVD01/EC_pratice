ECHO is on.

#### **`task-descriptions.md`**
```markdown
# Task Descriptions

## Task 1: GitHub Repository Setup and Version Control
- **Objective:** Learn to create a GitHub repository, use branching, and practice basic Git workflows.
- **Steps Completed:**
  1. Created a repository named `EC_pratice` with a `README.md` file.
  2. Added a `.gitignore` file to exclude unnecessary files.
  3. Created two branches: `feature-xy` and `bugfix-xy`.
  4. Made changes in both branches and pushed them to GitHub.

## Task 2: Merge Conflicts and Resolution
- **Objective:** Learn to simulate and resolve merge conflicts in Git.
- **Steps Completed:**
  1. Simulated a conflict by editing the same line in `README.md` in two branches.
  2. Resolved the conflict manually by merging both changes.
  3. Documented the process in `merge-conflict-details.md`.
